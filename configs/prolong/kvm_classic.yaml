model:
  # Layer this after a KVM model config to select the optimized classic
  # prefill/backward/training implementation and optimized cached decode.
  token_mixer_class_path: model.kvm_classic_mixer.SequenceMixer
