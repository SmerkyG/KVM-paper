model:
  # Use the packaged Triton 3.4 gfx942 forward code objects for their exact
  # compatible specialization; other shapes use the source-JIT fallback.
  kvm_aotriton_forward_attention: 1
  kvm_aotriton_precompiled_forward: 1
